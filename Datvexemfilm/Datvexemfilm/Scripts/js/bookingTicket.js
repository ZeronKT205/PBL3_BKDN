const btn_reset_DetilBooking = document.querySelector('.MainContainer__DetailBooking-btn-submit');
btn_reset_DetilBooking.addEventListener("click", function (event) {
    const _numseat = document.querySelector('.movie-detail-value seat-list');
    const total = document.querySelector('.movie-detail-value price total-price');
    const reponse = await fetch("http")
});
